# Pandas DataFrame from the following dictionary, where the keys represent
# columns and the values represent the data

data = { 'Name': ['Alice', 'Bob', 'Charlie', 'David'], 'Age': [25, 30, 22, 28], 'City': ['New York',
'London', 'Paris', 'Tokyo'] }

df = pd.DataFrame(data)
